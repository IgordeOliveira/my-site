---
const { repos } = Astro.props;
---

<div
  class="w-full max-w-fit p-4"
>
  <div class="flex items-center justify-between mb-3">
    <h5
      class="font-semibold font-mono text-white/90"
    >
      My Repositories
    </h5>
    <a
      href="https://github.com/IgordeOliveira?tab=repositories"
      target="_blank"
      class="btn btn-xs btn-primary"
    >
      View all →
    </a>
  </div>
  <ul class="my-4 space-y-3">
    {
      repos.map((repo: any) => (
        <li>
          <a
            href={repo.url}
            target="_blank"
            class="flex items-center p-2 font-bold rounded-sm bg-neutral-50/90 hover:translate-x-1 transition ease-in-out delay-50"
          >
            <div class="flex flex-col pl-2">
              <h1 class="col-2" class="font-semibold">
                {repo.name}
              </h1>
              <p class="font-normal text-sm">{repo.description}</p>
            </div>
          </a>
        </li>
      ))
    }
  </ul>
</div>
